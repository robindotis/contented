          <time datetime="{{ theDate | date }}" class="dt-published">
            <span class="date-day">{{ theDate | date("d") }}</span> 
            <span class="date-month">{{ theDate | date("F") }}</span>, 
            <span class="date-year">{{ theDate | date("Y") }}</span>
          </time>
