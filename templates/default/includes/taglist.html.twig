  <div class="tagged"><strong>Tagged: </strong>
    {%- for tag in tags  -%} {#| filterTagList#}
    {%- set tagUrl %}/tagging/{{ tag | slug }}/{% endset -%}
    <a href="{{ tagUrl }}" class="post-tag" rel="tag">{{ tag }}</a> &nbsp;
    {%- endfor %}
  </div>
